# Patches for Validity/Synaptics VFS0097 (VCSFW)

These are WIP upstream merge requests (libfprint !579 and !619), not a local
patch series. The driver code lives in the MR branches:

- https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/579
- https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/619

If/when you export a patch series from one of those branches, drop the numbered
`.patch` files here and note the base commit and which MR they came from.
