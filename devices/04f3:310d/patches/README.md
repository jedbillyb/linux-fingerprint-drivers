# Patches for ELAN ELAN7006 (04f3:310d)

This is a WIP upstream merge request (libfprint !383), not a local patch series.
The draft driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/383

It is not functional yet. If/when a working patch series is exported, drop the
numbered `.patch` files here and note the base commit.
