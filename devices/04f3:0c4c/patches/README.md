# Patches for ELAN elanmoc2 (04f3:0c4c, 04f3:0c00)

No standalone patch files are mirrored here yet. The driver is maintained as a
branch, not a patch series - build the `elanmoc2` branch linked in `../CREDITS`
directly.

If you generate a patch series against a known libfprint base
(`git format-patch <base>..elanmoc2`), drop the numbered `.patch` files here and
note the base commit so the fix can be reproduced against a plain checkout.
