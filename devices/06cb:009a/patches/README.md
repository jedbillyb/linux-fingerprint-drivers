# Patches for Synaptics Prometheus / python-validity (06cb:009a)

Not applicable in the usual sense: python-validity is a standalone userspace
daemon, not a libfprint patch series. Install it from the packages linked in
`../CREDITS` (Ubuntu PPA, Arch AUR, Fedora COPR) together with open-fprintd.

This directory is kept for consistency with the repo layout.
