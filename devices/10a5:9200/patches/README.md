# Patches for FPC1022 (FPC Disum, USB)

This is a WIP upstream merge request (libfprint !570), not a local patch series.
The driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/570

If/when you export a patch series from that branch, drop the numbered `.patch`
files here and note the base commit.
