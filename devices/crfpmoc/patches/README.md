# Patches for Chromium OS EC FPMCU (match-on-chip)

This is a WIP upstream merge request (libfprint !512), not a local patch series.
The driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/512

If/when you export a patch series from that branch, drop the numbered `.patch`
files here and note the base commit.
