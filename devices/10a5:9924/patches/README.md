# Patches for FPC match-on-host (10a5:9924)

This is a WIP upstream merge request (libfprint !611), not a local patch series.
The driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/611

If/when you export a patch series from that branch, drop the numbered `.patch`
files here and note the base commit.
