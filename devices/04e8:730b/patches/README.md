# Patches for Samsung 730B (image)

This is a WIP upstream merge request (libfprint !556), not a local patch series.
The driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/556

If/when you export a patch series from that branch, drop the numbered `.patch`
files here and note the base commit.
