# Patches for the Synaptics Tudor family (06cb:00ff and relatives)

No patch files are mirrored here. Both routes are full projects rather than patch
series against libfprint, and route 2 additionally depends on vendor Windows
driver files that cannot be redistributed. See `../CREDITS` for the sources.
