# Patches for Broadcom ControlVault3 (Dell)

This is a WIP upstream merge request (libfprint !620), not a local patch series.
The driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/620

If/when you export a patch series from that branch, drop the numbered `.patch`
files here and note the base commit. The proprietary Broadcom firmware blob must
NOT be committed here.
