# Patches for Goodix experimental driver (27c6:5110 and family)

No standalone patch files are mirrored here. This is an experimental, fast-moving
fork - build directly from the goodix-fp-linux-dev libfprint repo (see
`../CREDITS`). Pinning a patch series against a moving base is not useful while
the driver is unstable.
