# Patches for FocalTech (2808:9e65)

No standalone patch files are mirrored here yet. The driver is maintained as a
full repo (see `../CREDITS`); build it directly.

If you generate a patch series against a known libfprint base, drop the numbered
`.patch` files here and note the base commit.
