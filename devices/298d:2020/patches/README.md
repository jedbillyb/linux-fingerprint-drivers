# Patches for NextBiometrics NB-2020-U (nb1010)

This is a WIP upstream merge request (libfprint !569), not a local patch series.
The driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/569

If/when you export a patch series from that branch, drop the numbered `.patch`
files here and note the base commit.
