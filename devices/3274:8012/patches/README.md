# Patches for Microarray match-on-chip (PID 8012)

This is a WIP upstream merge request (libfprint !492), not a local patch series.
The driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/492

If/when you export a patch series from that branch, drop the numbered `.patch`
files here and note the base commit.
