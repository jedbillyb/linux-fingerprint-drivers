# Patches for FocalTech FT9365 ESS (focaltech_moc)

This is a WIP upstream merge request (libfprint !554), not a local patch series.
The driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/554

If/when you export a patch series from that branch, drop the numbered `.patch`
files here and note the base commit.
