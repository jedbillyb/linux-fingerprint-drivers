# Patches for EgisTec 0576

This is a WIP upstream merge request (libfprint !571), not a local patch series.
The driver code lives in the MR branch:
https://gitlab.freedesktop.org/libfprint/libfprint/-/merge_requests/571

If/when you export a patch series from that branch, drop the numbered `.patch`
files here and note the base commit.
