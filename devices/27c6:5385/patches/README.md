# Patches for Goodix HTK32 (27c6:5385 / 5335 / 5395)

No standalone patch files are mirrored here yet. The driver is distributed as a
full repo with an `install.sh` (see `../CREDITS`), built against libfprint
v1.94.10.

If you generate a patch series against a plain libfprint v1.94.10 checkout, drop
the numbered `.patch` files here and note the base commit/version.
