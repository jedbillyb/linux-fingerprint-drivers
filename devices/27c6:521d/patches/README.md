# Patches for Goodix 521d (27c6:521d / 538d)

No standalone patch files are mirrored here. The driver is distributed via the
`libfprint-goodix-521d` AUR package and the goodix-fp-linux-dev source (see
`../CREDITS`); build from there.

If you export a patch series against a known libfprint base, drop the numbered
`.patch` files here and note the base commit/version.
