# Patches for Validity/Synaptics VFS0090 (138a:0090 / 138a:0097)

No standalone patch files are mirrored here yet. The driver is maintained as the
`vfs0090` branch (see `../CREDITS`); build that branch directly, or use the
packaged builds (Ubuntu PPA, Arch AUR, nixpkgs).

If you generate a patch series against a known libfprint base, drop the numbered
`.patch` files here and note the base commit.
